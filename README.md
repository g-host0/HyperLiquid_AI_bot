# README.md

## 🤖 Hyperliquid AI Trading Bot

Автоматизированный торговый бот для биржи [Hyperliquid](https://hyperliquid.xyz) с AI-анализом рынка, продвинутым управлением рисками и многоуровневой системой Take Profit.

***

## 🎯 Основные возможности

### AI-Анализ
- **Двухуровневая верификация сигналов**: Level 1 (Grok 4.1 Fast) → Level 2 (DeepSeek V3.2) для подтверждения
- **Технический анализ**: EMA (10/20/50/100/200), RSI, MACD, Stochastic, StochRSI, Williams %R
- **Мультитаймфреймовый анализ**: 1d, 1h, 1m свечи с индикаторами перекупленности/перепроданности
- **Умное принятие решений**: выбор ОДНОГО актива с наиболее сильным сигналом

### Управление Рисками
- **ATR-based Stop Loss**: динамический SL на основе волатильности (ATR × 1.5)
- **Breakeven после TP1**: SL переносится на Entry Price после первого Take Profit
- **Прогрессивная система TP**: TP1 (30% позиции при +1%) → TP2 (20% остатка каждые +1%)
- **Защита от переторговки**:
  - 30 минут запрета на добор после TP
  - 90 минут запрета на переоткрытие после SL
- **Лимиты позиций**: максимум 400% от баланса, контроль доступных средств

### Автоматизация
- **Синхронизация с биржей**: автоматическое отслеживание позиций, ордеров и событий
- **Логирование событий**: SQLite база данных для трекинга TP/SL и истории сделок
- **Умное управление ордерами**: автоматическое обновление SL/TP при изменении размера позиции
- **Обнаружение добора**: корректировка параметров при увеличении позиции

***

## 📋 Требования

```txt
requests==2.32.3
numpy==2.2.1
python-dotenv==1.0.1
hyperliquid-python-sdk==0.7.7
eth-account==0.13.4
```

### Python версия
Python 3.9+ (протестировано на 3.12)

***

## 🚀 Установка

### 1. Клонирование репозитория
```bash
git clone https://github.com/g-host0/HyperLiquid_AI_bot.git
cd HyperLiquid_AI_bot
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка окружения
Создайте файл `.env` в корневой директории:

```env
# Hyperliquid
HYPERLIQUID_ACCOUNT_ADDRESS=0xYourAddress
HYPERLIQUID_PRIVATE_KEY=0xYourPrivateKey

# OpenRouter AI
OPENROUTER_API_KEY=your_openrouter_key
```

### 4. Инициализация базы данных
```bash
python init_db.py
```

***

## ⚙️ Конфигурация

### `config.py` - Основные параметры

#### Hyperliquid
```python
USE_TESTNET = True  # True для тестовой сети
```

#### AI Models
```python
ENABLE_TWO_LEVEL_VERIFICATION = True
OPENROUTER_MODEL_LEVEL1 = "x-ai/grok-4.1-fast"      # Быстрый анализ
OPENROUTER_MODEL_LEVEL2 = "deepseek/deepseek-v3.2"  # Верификация
```

#### Торговые параметры
```python
TEST_MODE = False  # True для симуляции без реальных ордеров
SYMBOLS = ["BTCUSDT", "ETHUSDT", "SOLUSDT", "BNBUSDT", "DOGEUSDT"]
MAX_SYMBOLS = 5
INTERVAL = 60  # Секунды между циклами

# Размер позиции
POSITION_SIZE_PERCENT = 100.0       # % от доступного баланса
MAX_TOTAL_POSITION_PERCENT = 400.0  # Максимальная сумма всех позиций

# Stop Loss
ATR_MULTIPLIER = 1.5  # SL = Entry Price ± (ATR × 1.5)

# Take Profit
TAKE_PROFIT_1_PERCENT = 1.0       # +1% для TP1
TAKE_PROFIT_1_SIZE_PERCENT = 30.0 # 30% позиции
TAKE_PROFIT_2_PERCENT = 1.0       # +1% шаг для TP2
TAKE_PROFIT_2_SIZE_PERCENT = 20.0 # 20% остатка каждый раз

# Защита от переторговки
ENABLE_NO_ADD_AFTER_TP = True
NO_ADD_AFTER_TP_MINUTES = 30      # Запрет добора 30 мин после TP

ENABLE_NO_REOPEN_AFTER_SL = True
NO_REOPEN_AFTER_SL_MINUTES = 90   # Запрет переоткрытия 90 мин после SL
```

***

## 🎮 Использование

### Запуск бота
```bash
python trading_bot.py
```

### Тестовый режим (без реальных ордеров)
Измените в `config.py`:
```python
TEST_MODE = True
TEST_BALANCE = 1000.0
```

### Остановка бота
Нажмите `Ctrl+C` для корректного завершения с отображением открытых позиций.

***

## 📊 Логика работы

### 1. Цикл бота (каждые 60 секунд)
```
Проверка баланса → Получение данных → AI анализ → Проверка сигналов → 
Размещение ордеров → Управление позициями → Отображение статуса
```

### 2. Размещение ордера
```
Проверка cooldown (SL/TP) → Расчёт размера (ATR) → Market ордер → 
Установка SL (ATR-based) → Установка TP1 (+1%, 30%)
```

### 3. Управление позицией

#### После TP1 (30% закрыто при +1%)
- ✅ SL → Entry Price (безубыток)
- ✅ TP2 устанавливается на +2% (20% остатка)

#### После TP2 (20% закрыто при +2%)
- ✅ TP3 устанавливается на +3% (20% остатка)
- ✅ И так далее прогрессивно

#### Добор к позиции
- Пересчёт `original_quantity` при обнаружении увеличения размера
- Обновление SL на новый размер позиции
- Запрет на 30 минут после срабатывания TP

### 4. Закрытие позиции

#### По SL
- Логирование события в `trade_events`
- Запрет переоткрытия в том же направлении на 90 минут
- Статус `closed`, причина `sl`

#### По TP
- Полное закрытие после нескольких TP2
- Статус `closed`, причина `tp`

***

## 📁 Структура проекта

```
hyperliquid-bot/
├── config.py              # Конфигурация
├── hyperliquid_api.py     # API Hyperliquid
├── utils.py               # Технический анализ, AI запросы
├── trading_bot.py         # Основная логика бота
├── init_db.py             # Инициализация БД
├── requirements.txt       # Зависимости
├── .env                   # Приватные ключи (не коммитить!)
├── positions.db           # SQLite база данных
└── README.md              # Документация
```

***

## 🗄️ База данных

### Таблица `positions`
```sql
id, symbol, side, quantity, entry_price, position_value, atr,
stop_loss, stop_loss_percent, original_quantity, tp1_hit, tp2_hit, 
tp2_count, last_known_size, status, profit, opened_at, closed_at, 
close_reason
```

### Таблица `trade_events`
```sql
id, symbol, event_type (tp/sl), side (long/short), 
event_time, details
```

***

## 🛡️ Система защиты

| Функция | Описание | Параметр |
|---------|----------|----------|
| **ATR Stop Loss** | Динамический SL на основе волатильности | `ATR × 1.5` |
| **Breakeven SL** | После TP1 SL переносится на Entry Price | Автоматически |
| **Cooldown TP** | Запрет добора после Take Profit | 30 минут |
| **Cooldown SL** | Запрет переоткрытия после Stop Loss | 90 минут |
| **Max Position** | Лимит размера всех позиций | 400% баланса |
| **Available Balance** | Учёт только свободных средств | Автоматически |

***

## 📈 Пример работы

### Открытие позиции LONG BTCUSDT
```
💰 Баланс: $1000.00 | Доступно: $1000.00
🎯 buy_BTCUSDT | Strong momentum, RSI oversold

📤 BUY 0.0200 BTC
✅ Исполнено: 0.0200 @ $50000.00
✅ SL установлен по ATR @ $49250.00
✅ TP1 установлен @ $50500.00 (30%)
```

### После TP1 (+1%)
```
✅ BTC: TP1 сработал (70.0% осталось)
✅ Управление позициями: обновлено 2
  └─ SL: $50000.00 (безубыток)
  └─ TP: $51000.00 (+2%, 20%)
```

### После TP2 (+2%)
```
✅ BTC: TP2 сработал (56.0% осталось)
✅ Управление позициями: обновлено 1
  └─ TP: $51500.00 (+3%, 20%)
```

***

## ⚠️ Важные примечания

### Риски
- Криптовалютная торговля сопряжена с высоким риском
- Используйте **тестовую сеть** для обучения
- Начинайте с **минимальных сумм** на реальной сети
- **Никогда не инвестируйте** средства, которые не можете потерять

### Безопасность
- **НЕ КОММИТЬТЕ** `.env` файл в Git
- Храните приватные ключи в безопасном месте
- Используйте отдельный аккаунт для бота
- Регулярно проверяйте логи и открытые позиции

### Ограничения
- Бот работает только с перпетуальными контрактами Hyperliquid
- Минимальный размер позиции зависит от актива (проверяйте `szDecimals`)
- API rate limits: делайте паузы между запросами (уже встроено)
- AI анализ зависит от качества данных и промптов

***

## 🔧 Troubleshooting

### Ошибка "Hyperliquid credentials не установлены"
- Проверьте наличие `.env` файла
- Убедитесь, что переменные `HYPERLIQUID_ACCOUNT_ADDRESS` и `HYPERLIQUID_PRIVATE_KEY` заполнены

### Ошибка "OpenRouter API ключ не найден"
- Добавьте `OPENROUTER_API_KEY` в `.env`
- Получите ключ на [OpenRouter](https://openrouter.ai/)

### Ордер не исполняется
- Проверьте баланс: `💰 Баланс: $X.XX | Доступно: $Y.YY`
- Убедитесь, что `POSITION_SIZE_PERCENT` корректен
- Проверьте минимальный размер ордера для актива

### SL/TP не устанавливаются
- Проверьте, что позиция открыта на бирже
- Проверьте логи на ошибки `set_sl_only` / `set_tp_only`
- Убедитесь, что цены SL/TP не слишком близки к текущей цене

### База данных повреждена
```bash
rm positions.db
python init_db.py
```

***

## 📝 Логи и мониторинг

### Вывод в консоли
```
🤖 ТОРГОВЫЙ БОТ Hyperliquid
🔴 РЕАЛЬНЫЙ РЕЖИМ
💰 Баланс: $1000.00 | Доступно: $1000.00
📊 TP1: +1% (30% позиции)
📊 TP2: +1% (20% остатка)
🚫 Запрет добора после TP: 30 мин
🚫 Запрет переоткрытия после SL: 90 мин

🧠 АНАЛИЗ AI
🔍 Уровень 1 (x-ai/grok-4.1-fast): первичный анализ...
✅ Уровень 2 (deepseek/deepseek-v3.2): подтверждение сигнала...
🎯 buy_ETHUSDT | Подтверждено: Bullish divergence on RSI

📊 ОТКРЫТЫЕ ПОЗИЦИИ
ETH LONG: 1.5000 @ $3500.00 | $5250.00 | P&L +1.2% (+$63.00)
  └─ SL: $3450.00 (100%, объём 1.5000)
  └─ TP: $3535.00 (30%, объём 0.4500)
```

### SQLite запросы
```sql
-- История событий
SELECT * FROM trade_events ORDER BY event_time DESC LIMIT 10;

-- Открытые позиции
SELECT * FROM positions WHERE status='open';

-- Закрытые позиции по SL
SELECT * FROM positions WHERE close_reason='sl' ORDER BY closed_at DESC;
```

***

## 🌟 Roadmap

- [ ] Telegram уведомления
- [ ] Web dashboard для мониторинга
- [ ] Бэктестинг на исторических данных
- [ ] Дополнительные AI модели (Claude, GPT-4)
- [ ] Поддержка spot торговли
- [ ] Trailing Stop Loss
- [ ] Настраиваемые стратегии через YAML

***

## 📄 Лицензия

MIT License - используйте на свой страх и риск.

***

## 🙏 Благодарности

- [Hyperliquid Python SDK](https://github.com/hyperliquid-dex/hyperliquid-python-sdk)
- [OpenRouter](https://openrouter.ai/) за доступ к AI моделям
- [X AI (Grok)](https://x.ai/) и [DeepSeek](https://www.deepseek.com/)

***

## 📞 Поддержка

При возникновении проблем:
1. Проверьте **Troubleshooting** секцию
2. Изучите логи бота
3. Проверьте конфигурацию в `config.py`
4. Убедитесь в корректности `.env` файла

**Успешной торговли! 🚀**
